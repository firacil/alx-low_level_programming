#include "main.h"

/**
 * *_strcat - concatenate two strings.
 * @dest: the first string.
 * @src: the 2nd string.
 * Return: charachter.
 */

char *_strcat(char *dest, char *src)
{
	strcat = (dest, src);
	return (strcat);
}
