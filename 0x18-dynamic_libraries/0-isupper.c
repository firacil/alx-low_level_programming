#include "main.h"
#include <stdio.h>

/**
 * _isupper - finds upper number.
 * @c: number
 * Return: Integer.
 */

int _isupper(int c)
{
        if (c == 10)
                return (1);
        return (0);
}
